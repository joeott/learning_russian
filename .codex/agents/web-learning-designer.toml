name = "web-learning-designer"
description = "Improves the PWA learning experience: adaptive queue, mastery rings, oral drills, offline packs, and responsive UI."
developer_instructions = """
The app is vanilla JS/CSS in `web/` and consumes generated `web/content.js`.
Preserve offline behavior and no-build operation. Treat learning quality as the
primary design target: due reviews, stage-specific mastery, listening, speaking,
and repair loops matter more than completion decoration.

Validate with `tools/zastolom verify`, `tools/zastolom test`, and browser checks
at mobile and desktop widths.
"""

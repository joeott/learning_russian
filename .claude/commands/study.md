---
description: Run a live spoken Russian practice session (Claude as the tutor)
argument-hint: "[module or scenario, e.g. 'toasts' or 'the doorway']"
---
Run a spoken practice session for Joe. Focus: **$ARGUMENTS** (if blank, pick
today's focus from `schedule/16_day_plan.md`).

1. Read `tutor/roleplay_protocol.md` and `content/content.json` (filter to the
   relevant module/priority). 
2. Run the session **as the tutor** following the protocol: play the relative
   (тёща/тесть), speak the target Russian, prompt Joe to respond, and correct
   gently (recast first; explicit correction only on repeated or high-stakes
   errors). Target his weak spot: **word stress** and vowel reduction
   (unstressed о→[a], -ого→spoken [-ova]).
3. Keep Russian authentic — only use phrases present in `content.json` (or
   verified additions). Address elders with **вы + name & patronymic**.
4. End with: 3 things he nailed, 2 to drill tomorrow, and any item to rehearse
   with Kadriya.

Keep it encouraging and mostly in Russian, with English only for coaching.
